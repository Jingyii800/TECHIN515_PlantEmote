import pyaudio

p = pyaudio.PyAudio()

for i in range(p.get_device_count()):
  info = p.get_device_info(i)
  print(f"Device {i+1}: {info['name']}, Channels: {info['maxInputChannels']}")

p.terminate()
